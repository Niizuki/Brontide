# `binding/portable/vectors/`

Valid, additive-compatible, and adversarial fixtures with exact expected outcomes, introduced in
**PB1** (plan §5). Adversarial coverage includes unknown fields in control/authority positions,
unknown variants, duplicate fields, version skew, malformed data, mismatched correlation, replay
where declared, limit violations, illegal lifecycle transitions, incompatible Shapes/Fragments,
forbidden resource scope, and exception-shaped data.

These are data only and must validate without loading either stack.

Empty until PB1.
