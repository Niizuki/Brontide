{
  "schemaVersion": 1,
  "targetFramework": "net10.0",
  "minimumInclusive": "10.0.100",
  "maximumExclusive": "11.0.0",
  "allowPrerelease": true,
  "ciFeatureBands": ["10.0.100", "10.0.4xx"],
  "rationale": "Brontide intentionally follows the caller-selected .NET 10 SDK rather than pinning one patch with global.json. CI covers the first and current .NET 10 feature bands; local prerelease SDKs inside the same major range remain usable for architecture development."
}
