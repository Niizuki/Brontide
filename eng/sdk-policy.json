{
  "schemaVersion": 1,
  "targetFramework": "net10.0",
  "minimumInclusive": "10.0.100",
  "maximumExclusive": "11.0.0",
  "allowPrerelease": true,
  "ciFeatureBands": ["10.0.100", "10.0.x"],
  "rationale": "Brontide intentionally follows the caller-selected .NET 10 SDK rather than pinning one patch with global.json. CI covers the first exact .NET 10 SDK and the current .NET 10 channel at preview quality; local prerelease SDKs inside the same major range remain usable for architecture development."
}
